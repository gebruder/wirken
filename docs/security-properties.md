# Security Properties

Designed against the [OWASP Top 10 for Agentic AI](https://genai.owasp.org/resource/agentic-ai-threats-and-mitigations/).

| OWASP | Threat | Mitigation |
|-------|--------|------------|
| AG01 | Excessive agency | Three-tier permission model. Tier 1 (always allowed): workspace file access, web search. Tier 2 (first-use approval, remembered 30 days): external file access, cross-conversation message, and a curated allowlist of shell-inspection verbs (ls / cat / grep / stat / pwd / whoami / ... — no documented exec escape hatch). Tier 3 (always prompt): destructive ops, credential access, network requests, skill install, and every shell verb outside the Tier 2 allowlist — shell wrappers, language interpreters with -c/-e eval, and build/deploy tools all default to Tier 3. See [permissions-and-identity.md](permissions-and-identity.md). |
| AG02 | Code execution | Docker sandbox: ephemeral containers, no-network, 512MB memory, 256 PID limit, non-root user. gVisor sandbox: same constraints with kernel attack surface reduction via `runsc` runtime. Wasm sandbox: compiled skill modules run in Wasmtime with fuel-based CPU limits, no filesystem, no network. Shell exec timeout at 300s. **Operator-controlled escape:** `sandbox.json` set to `mode: off` runs `exec` directly on the host at the wirken UID. The host shell can then read and rewrite every trust file under the data dir (`tool_policy.json`, `sandbox.json`, `org.url`, `org-config-pubkey.pub`, `audit.db`, `vault.db`, `agents/<id>/identity.key`); changes take effect at the next gateway start. The gateway emits a `tracing::warn!` at startup when `sandbox.json` is `mode: off`. Treat this as a deliberate operator opt-out, not a default posture. **MCP child trust:** MCP servers configured in `mcp.json` spawn at the wirken UID with no process sandbox. The agent's permission gate checks the MCP tool *name* but not the child's own behavior once spawned; a child can read `audit.db` / `vault.db` / identity keys, open outbound connections, or spawn further processes — all at the wirken UID. Install MCP servers only from trusted sources. The gateway lists configured MCP server names at startup so the inventory is operator-visible. |
| AG04 | Tool misuse | Tool inputs validated against JSON schema. Workspace path confinement: file operations are canonicalized and rejected if outside workspace boundary. |
| AG05 | Identity spoofing | Per-adapter Ed25519 challenge-response handshake over the local IPC transport (Unix domain sockets on Linux/macOS, Windows named pipes on Windows); signature covers `(domain || adapter_id || nonce)` so the signed payload binds the identity cryptographically, not only through the pubkey-registration lookup. After handshake the gateway resolves the adapter's channel from the registry and wraps it in `AuthenticatedChannel`; every inbound frame's self-declared channel is matched against that value, and cross-channel frames are rejected with an `adapter.channel_mismatch` audit event. Compile-time channel isolation: `SessionHandle<Telegram>` and `SessionHandle<Discord>` are different types; the compiler rejects cross-channel access. |
| AG07 | Multi-agent manipulation | Each channel adapter runs as a separate OS process. If an adapter is compromised, the blast radius is one channel. IPC boundary prevents lateral movement. Child agents spawned via `spawn_subagent` run under capability-attenuated ceilings: tool allowlist intersection, clamped permission tier, max rounds, max runtime, headless (no interactive approvals). Hard depth cap of 4 prevents nesting cycles. |
| AG08 | Runaway loops | Agent tool call loop capped at 20 rounds per turn. Child agents have a separate per-ceiling `max_rounds` budget. Shell exec timeout at 300s. Rate limiting on all sources including loopback, with no localhost exemption. |
| AG09 | Insufficient logging | Every agent action logged as a typed session event to an append-only, per-session hash-chained SQLite table before execution. Per-agent Ed25519 attestation signs the chain head after every turn. `wirken session verify` replays the session log offline and re-checks hashes. 90-day retention with configurable pruning. Real-time SIEM forwarding to Datadog, Splunk HEC, Microsoft Sentinel (Logs Ingestion API), or webhook. Prompt injection detection flags inbound messages with threat indicators. Permission denials logged with full context: tool, tier, agent, trigger message. |
| | Credential security | XChaCha20-Poly1305 encryption at rest, keyed from OS keychain on Linux/macOS (macOS Keychain / libsecret) with an age-encrypted file fallback. Windows uses the age-file backend; native Credential Manager / DPAPI integration is on the roadmap. Per-credential expiry and rotation. `secrecy` + `zeroize` ensure that logging or serializing a secret is a compile error. Key material zeroed after use. |
| | Transport security | HTTPS enforced at transport level for all LLM and Matrix connections (non-localhost). Cap'n Proto IPC with 16MB frame limit, 512M word traversal limit, 64-level nesting limit. |
| | Supply chain | Skill signatures verified against registry-provided Ed25519 key, not a bundled key. Release binaries include SHA-256 checksums; installer verifies before installing. CI runs clippy with `-D warnings`, fmt check, and full test suite on every push. |
| | Confidential inference | Tinfoil and Privatemode providers run open-source LLMs inside hardware TEEs (AMD SEV-SNP, Intel TDX, NVIDIA H100 CC). Prompts encrypted end-to-end, protected against software attacks on infrastructure. |

## NIST AI Risk Management Framework mapping

The [NIST AI RMF (AI 100-1)](https://nvlpubs.nist.gov/nistpubs/ai/NIST.AI.100-1.pdf) takes the complementary view: how an organization governs, maps, measures, and manages AI risk across its lifecycle. The mapping below lists only RMF subcategories where Wirken ships a code-verifiable capability today. Subcategory text is defined in the companion [NIST AI RMF Playbook](https://airc.nist.gov/AI_RMF_Knowledge_Base/Playbook).

| Subcategory | Wirken Capability | Implementation |
|-------------|-------------------|----------------|
| GOVERN 1.1, policies and procedures | Three-tier permission model with first-use approval, expiry, and revocation | [`crates/gateway/src/permissions.rs:9-49`](../crates/gateway/src/permissions.rs) (tiers + `Action` enum), [`:105-138`](../crates/gateway/src/permissions.rs) (`tier()` resolver), [`:323-356`](../crates/gateway/src/permissions.rs) (`approve_by_key`, Tier 3 refusal at `:329-337`), [`:226-266`](../crates/gateway/src/permissions.rs) (`prune_non_tier2_shell_approvals` migration) |
| GOVERN 1.6, AI system inventory and lifecycle | Per-credential lifecycle metadata: `created_at`, `expires_at`, `last_used_at`, `rotation_due_at`; `rotate()` API | [`crates/vault/src/store.rs:16-19`](../crates/vault/src/store.rs) (struct fields), [`:25-30`](../crates/vault/src/store.rs) (`is_expired` / `rotation_due`), [`:270`](../crates/vault/src/store.rs) (`rotate`) |
| GOVERN 2.1, roles and responsibilities | Centralized org policy endpoint: provider, SIEM, MCP servers, sandbox mode pulled from a company URL and applied locally | [`crates/gateway/src/org.rs:139`](../crates/gateway/src/org.rs) (`apply_org_config`) |
| MAP 1.1, context and use cases | Model-agnostic provider routing across OpenAI, Anthropic, Gemini, Bedrock, Ollama, Tinfoil, Privatemode, and OpenAI-compatible endpoints | [`crates/agent/src/llm.rs:46-141`](../crates/agent/src/llm.rs) (per-provider factories: `openai`, `anthropic`, `gemini`, `bedrock`, `ollama`, `tinfoil`, `privatemode`) |
| MAP 5.1, impact and blast radius | Compile-time channel isolation: `SessionHandle<C: Channel>` is parameterized by a sealed marker type, so cross-channel access is a type error | [`crates/ipc/src/channel.rs:68`](../crates/ipc/src/channel.rs) (sealed `Channel` trait), [`:85-90`](../crates/ipc/src/channel.rs) (`SessionHandle<C>` typing rules); negative-test scaffolding at [`crates/ipc/src/tests.rs:20-30`](../crates/ipc/src/tests.rs) |
| MEASURE 2.5, output monitoring | Real-time SIEM forwarding to Datadog Log Intake, Splunk HEC, Microsoft Sentinel (Logs Ingestion API), or generic webhook, in addition to the local session log | [`crates/audit/src/siem.rs:26-43`](../crates/audit/src/siem.rs) (`SiemTarget` enum), [`:81-86`](../crates/audit/src/siem.rs) (forward dispatch) |
| MEASURE 2.6, security and resilience | Prompt injection detector flags inbound messages with threat metadata (role-switching, instruction overrides, base64 commands, tool-call injection); events are tagged in audit, not blocked | [`crates/gateway/src/injection_detect.rs:39-54`](../crates/gateway/src/injection_detect.rs) (`ThreatSeverity`), [`:94-148`](../crates/gateway/src/injection_detect.rs) (`InjectionDetector::scan`) |
| MEASURE 2.7, system logging | Append-only per-session hash-chained session log. Each event carries a SHA-256 leaf hash and chain hash. Per-agent Ed25519 attestation signs the chain head. `wirken session verify` replays events offline and re-checks hashes and tool results | [`crates/audit/src/session_log.rs:640-650`](../crates/audit/src/session_log.rs) (table schema), [`:659-770`](../crates/audit/src/session_log.rs) (`append`), [`:773-833`](../crates/audit/src/session_log.rs) (`verify`); attestation [`crates/agent/src/attestation.rs:86`](../crates/agent/src/attestation.rs) (`attest_session`), [`:134`](../crates/agent/src/attestation.rs) (`verify_session_attestations`) |
| MANAGE 1.3, risk mitigation | Three sandbox runtimes: Docker and gVisor confine the `exec` tool (no-network, 512 MB, 256 PID, non-root, 300 s); Wasmtime runs Wasm skills with fuel-based CPU limits, no filesystem, no network. See [sandbox-properties.md](sandbox-properties.md). | [`crates/agent/src/sandbox.rs:22-41`](../crates/agent/src/sandbox.rs) (`SandboxMode`), [`:287-319`](../crates/agent/src/sandbox.rs) (`build_host_config`); [`crates/agent/src/wasm_sandbox.rs:37-96`](../crates/agent/src/wasm_sandbox.rs) (`WasmSkill`), [`:62-67`](../crates/agent/src/wasm_sandbox.rs) (`consume_fuel`); plumbed through [`crates/agent/src/tool.rs:328-346`](../crates/agent/src/tool.rs) (no silent fallback) |
| MANAGE 2.2, input validation | Tool inputs declared as JSON Schema; filesystem tools constrained by `cap_std::Dir` rooted at the workspace, so any path traversal or absolute-path escape is rejected at the capability boundary | [`crates/agent/src/tool.rs:52`](../crates/agent/src/tool.rs) (`workspace_dir: Arc<cap_std::fs::Dir>`), [`:76`](../crates/agent/src/tool.rs) (`Dir::open_ambient_dir`); call sites [`:397`](../crates/agent/src/tool.rs) (`read_file`), [`:428`](../crates/agent/src/tool.rs) (`write_file`), [`:464`](../crates/agent/src/tool.rs) (`list_files`) |
| MANAGE 2.4, abuse and overuse limits | Auth rate limiter with no loopback exemption (5 failures / 60 s / 10-minute lockout) and a control-plane GCRA limiter via `governor` | [`crates/gateway/src/rate_limit.rs:10-23`](../crates/gateway/src/rate_limit.rs) (`AuthRateLimiter`, no-loopback-exemption note), [`:131-136`](../crates/gateway/src/rate_limit.rs) (`ControlPlaneRateLimiter` GCRA via `governor::RateLimiter`) |
